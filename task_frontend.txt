TASK: Create frontend src files for chat-realtime example

CONTEXT:
- Project: GraphQL Cascade monorepo
- Location: /home/lionel/code/graphql-cascade/examples/chat-realtime/frontend/src/
- Frontend: React with URQL

OBJECTIVE:
Create the frontend source files for the chat-realtime example.

FILES TO CREATE:
1. /home/lionel/code/graphql-cascade/examples/chat-realtime/frontend/src/main.tsx
2. /home/lionel/code/graphql-cascade/examples/chat-realtime/frontend/src/App.tsx
3. /home/lionel/code/graphql-cascade/examples/chat-realtime/frontend/src/urql-client.ts - URQL with WebSocket
4. /home/lionel/code/graphql-cascade/examples/chat-realtime/frontend/src/cascade-exchange.ts
5. /home/lionel/code/graphql-cascade/examples/chat-realtime/frontend/src/components/ChannelList.tsx
6. /home/lionel/code/graphql-cascade/examples/chat-realtime/frontend/src/components/MessageList.tsx
7. /home/lionel/code/graphql-cascade/examples/chat-realtime/frontend/src/components/MessageInput.tsx

FRONTEND IMPLEMENTATION:
```typescript
// urql-client.ts
import { createClient, subscriptionExchange, fetchExchange } from 'urql';
import { createClient as createWSClient } from 'graphql-ws';
import { cascadeExchange } from './cascade-exchange';

const wsClient = createWSClient({
  url: 'ws://localhost:4000/graphql',
});

export const client = createClient({
  url: 'http://localhost:4000/graphql',
  exchanges: [
    cacheExchange,
    cascadeExchange,
    fetchExchange,
    subscriptionExchange({
      forwardSubscription: (operation) => ({
        subscribe: (sink) => ({
          unsubscribe: wsClient.subscribe(operation, sink),
        }),
      }),
    }),
  ],
});
```

VERIFICATION:
- ls -la /home/lionel/code/graphql-cascade/examples/chat-realtime/frontend/src/

ACCEPTANCE CRITERIA:
- [ ] URQL client with subscription exchange
- [ ] Cascade exchange processes subscription cascade data

DO NOT:
- Use polling instead of subscriptions
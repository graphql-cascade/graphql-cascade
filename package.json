{
  "name": "graphql-cascade-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/client-core/core",
    "packages/client-apollo/apollo",
    "packages/client-react-query/react-query",
    "packages/client-relay/relay",
    "packages/client-urql/urql",
    "packages/cli",
    "packages/conformance",
    "packages/server-node"
  ],
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck",
    "clean": "rm -rf packages/*/dist packages/**/node_modules node_modules",
    "diagrams": "mmdc -p puppeteer-config.json -i docs/diagrams/manual-cache-management.mmd -o docs/diagrams/manual-cache-management.png && mmdc -p puppeteer-config.json -i docs/diagrams/automatic-cache-management.mmd -o docs/diagrams/automatic-cache-management.png && mmdc -p puppeteer-config.json -i docs/diagrams/how-it-works.mmd -o docs/diagrams/how-it-works.png && mmdc -p puppeteer-config.json -i docs/diagrams/cascade-flow.mmd -o docs/diagrams/cascade-flow.png && mmdc -p puppeteer-config.json -i docs/diagrams/entity-relationships.mmd -o docs/diagrams/entity-relationships.png",
    "version": "changeset version",
    "release": "pnpm run build && changeset publish",
    "sbom": "pnpm dlx @cyclonedx/cyclonedx-npm --output-file sbom.json --ignore-npm-errors"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}